<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/zy_primary</item>
        <item name="colorPrimaryDark">@color/zy_background</item>
        <item name="colorAccent">@color/zy_primary</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/zy_surface</item>
    </style>

    <!-- 自定义 Dialog 样式 -->
    <style name="BaseDialog" parent="android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>  <!-- 没有标题-->
        <item name="android:windowBackground">@android:color/white</item>   <!-- 背景颜色 -->
        <item name="android:windowFrame">@null</item>   <!-- 没有边框 -->
        <item name="android:windowIsFloating">true</item>   <!-- 窗口浮动 -->
        <item name="android:windowContentOverlay">@null</item>  <!-- 没有阴影 -->
    </style>

    <!-- 隐藏标题栏和设置状态栏透明 -->
    <style name="TransparentStatusBarTheme" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>


    <style name="CTheme" parent="TransparentStatusBarTheme">
        <item name="android:windowFullscreen">true</item>   <!--全屏-->
    </style>


    <style name="ZySettingsRow">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">64dp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:paddingStart">18dp</item>
        <item name="android:paddingEnd">14dp</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
    </style>

    <style name="ZySettingsIcon">
        <item name="android:layout_width">28dp</item>
        <item name="android:layout_height">28dp</item>
        <item name="android:layout_marginEnd">16dp</item>
        <item name="android:scaleType">centerInside</item>
    </style>

    <style name="ZySettingsTitle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@color/zy_text_primary</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="ZySettingsChevron">
        <item name="android:layout_width">28dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/zy_text_tertiary</item>
        <item name="android:textSize">28sp</item>
    </style>

</resources>
